/*
 * Copy this file to supabase-config.js and set the two public values from
 * Supabase Dashboard → Project Settings → API.  Do not put a service-role
 * key in this application: it would be visible to every person using it.
 *
 * supabase-config.js is deliberately ignored by git.  The app still works in
 * its existing Google Sheets/offline mode when this file is absent.
 */
window.FLAGSHIP_SUPABASE_CONFIG = {
  url: 'https://YOUR_PROJECT.supabase.co',
  anonKey: 'YOUR_SUPABASE_ANON_KEY'
};
